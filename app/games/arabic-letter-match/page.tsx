import { ArabicLetterMatch } from '@/components/games/ArabicLetterMatch';

export const metadata = {
  title: 'Arabic Letter Match - Mam',
  description: 'A fun memory game to learn Arabic letters',
};

export default function ArabicLetterMatchPage() {
  return <ArabicLetterMatch />;
}
